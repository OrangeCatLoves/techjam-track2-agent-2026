[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: runs a full training or full-data job (minutes). Skip with -m "not slow" during fast iteration; CI must run them.
    data: needs the KuaiRand-Pure CSVs on disk.
