# Reproduce the official baseline with numpy alone before installing anything below.
# The starter kit is numpy-only by design; adding libraries is allowed by the rules
# but must not change the baseline number.

numpy==1.26.4

# Core stack
pandas==2.2.2
pyarrow==16.1.0
lightgbm==4.3.0
scipy==1.13.1
scikit-learn==1.5.0

# Config, logging, testing
PyYAML==6.0.1
pytest==8.2.0

# Cross-platform process monitoring (memory ceilings on Windows,
# where the POSIX `resource` module is unavailable)
psutil==5.9.8

# LLM provider. Keep only the one you use, and record token counts from
# the response object on every call -- Feasibility scoring depends on it.
anthropic==0.34.2
# openai==1.40.0
